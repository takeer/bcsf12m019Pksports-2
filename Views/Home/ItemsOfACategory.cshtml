@model List<Practice1_Web.Models.item>
@{
    ViewBag.Title = "ItemsOfACategory";
}
@{
    string[] items = ViewBag.items;
}

<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h4><i class="fa fa-fw fa-check"></i>@ViewBag.headingTitle</h4>
                </div>
            </div>
        </div>
    </div>
    <div class="container">


        @*@for (int k = 0, j = 0; k < Model.Count ; j++)
        {

            <div class="row">
                @for (int i = 0; i < 4 && k < Model.Count; i++, k++)
                {

                    <div class="col-md-3">
                        <div class="panel panel-default">
                            <div class="panel-heading">
                                <h4><i class="fa fa-fw fa-check"></i><span>@Model[i].Name</span> </h4>
                            </div>
                            <div class="panel-body">
                                <a href="#" class="thumbnail">
                                    <img style="max-height:100px" src="@(Model[i].Pic)" class="img-rounded">
                                </a>

                                <p>
                                    <a href="#" class="btn btn-primary">View More</a>
                                    <a href="#" class="btn btn-default"><span class="glyphicon glyphicon-heart"></span>&emsp;Like</a>
                                </p>

                            </div>
                        </div>
                    </div>


                }

            </div>
        }*@


        @for (int k = 0, j = 0; k < Model.Count; j++)
        {
            <div class="row">
                @for (int i = 0; i < 4 && k < Model.Count; i++, k++)
                {
                    <div class="col-md-3">
                        <div class="panel panel-default">
                            <div class="panel-heading">
                                <h4><i class="fa fa-fw fa-check">@Model[i].Name</i> </h4>
                            </div>
                            <div class="panel-body">
                                <div align="center"><p style="align-items:center"><button type="button" class="btn btn-default disabled">Price Rs:<span>@Model[i].Price</span></button></p></div>
                                <a href="#"@*@("/Home/ItemsOfACategory/?type="+Model[i].Name)*@ class="thumbnail">
                                    <img style="max-height:100px" src=@("/Images/" + i % Model.Count + ".jpg") class="img-rounded">
                                </a>
                                <div align="center">
                                    <p>
                                        <a href="@("/Home/AddtoCart/?Id=" + Model[i].Id)" class="btn btn-primary">Add to cart</a>
                                        <a href="#" class="btn btn-default"><span class="glyphicon glyphicon-heart"></span>&emsp;Like</a>
                                    </p>

                                </div>

                            </div>
                        </div>
                    </div>
                }
            </div>
        }
    </div>
</div>






