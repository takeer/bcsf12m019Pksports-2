@{
    ViewBag.Title = "Home Page";
}
@{
    string[] bestsales = ViewBag.bestsales;
}
@model List<Practice1_Web.Models.item>

<div class="container">




    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <i class="fa fa-fw fa-check"></i><h4> <span class="btn btn-default btn-lg disabled"> @ViewBag.headingTitle</span>  </h4>
                </div>
            </div>
        </div>
    </div>
    @*thumbnail view*@
    <div class="container">
        @for (int k = 0, j = 0; k < Model.Count @*&& j < categories.Length/4+1*@; j++)
        {

            <div class="row">
                @for (int i = 0; i < 4 && k < Model.Count; i++, k++)
                {

                    <div class="col-md-3">
                        <div class="panel panel-default">
                            <div class="panel-heading">
                                <h4><i class="fa fa-fw fa-check">@Model[i].Name</i> </h4>
                            </div>
                            <div class="panel-body">
                                <a href="#" class="thumbnail">
                                    <img style="max-height:100px" src="@(Model[i].Pic)" class="img-rounded">
                                </a>

                                <p>
                                    <a href="#" class="btn btn-primary disabled">View More</a>
                                    <a href="#" class="btn btn-default"><span >12</span>&emsp;Likes</a>
                                </p>

                            </div>
                        </div>
                    </div>


                }

            </div>
        }
    </div>
</div>



